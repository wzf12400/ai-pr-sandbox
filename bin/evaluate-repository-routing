#!/bin/sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$ROOT"
exec python3 -c 'from src.repository_routing_benchmark import evaluate_main; raise SystemExit(evaluate_main())' "$@"
